#include "monty.h"

/**
 * free_stack - frees the stack on exit
 * @status: exit status
 *
 */
void free_stack(int status, void *arg)
{
	(void)status;
	(void)arg;
/*listint_t *ptr, *hold;

	if (head == NULL)
		return;

	ptr = head;

	while (ptr != NULL)
	{
		hold = ptr->next;
		free(ptr);
		ptr = hold;
	}*/

}
